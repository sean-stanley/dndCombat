// import { fromJS } from 'immutable';
// import { makeSelectEncounterDomain } from '../selectors';

// const selector = makeSelectEncounterDomain();

describe('makeSelectEncounterDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
