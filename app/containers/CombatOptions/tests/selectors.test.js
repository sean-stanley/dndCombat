// import { fromJS } from 'immutable';
// import { makeSelectCombatOptionsDomain } from '../selectors';

// const selector = makeSelectCombatOptionsDomain();

describe('makeSelectCombatOptionsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
